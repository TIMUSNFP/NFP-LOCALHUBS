# Files NOT to deploy to Vercel. The live app only needs:
#   participant/  circle-leaders/  admin/   (the 3 static sites)
#   api/  backend/                          (the serverless API + its source)
#   package.json  vercel.json
# Everything below is local tooling, old files, or assets the sites don't use.

# Dependencies (Vercel installs these fresh from package.json)
node_modules

# Secrets — never upload (also git-ignored, this is belt-and-suspenders)
backend/.env
.env

# Old single-page monolith (superseded by the 3 site folders)
index.html
script.js
styles.css
pin-editor.html
pin-editor-server.js
serve-static.js

# Old SQLite data + scratch folders
backend/data
data
projects
sites

# Shared root assets — each site folder has its own copy, so these are unused
Images
Fonts-20260608T063024Z-3-001

# Local tooling / editor / agent config
.jetro
.claude
.cursor
.agents
.github
.windsurfrules
.mcp.json

# Docs
*.md
